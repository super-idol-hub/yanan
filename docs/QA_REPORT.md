# QA 状态

当前状态：**候选包自动验证通过；人工验收尚未完成，尚未正式发布**。

- 已阅读组织规范，已确认人物身份与“黑白日常”形象。
- C# 5 / .NET Framework 源码编译通过，警告视为错误。
- 完成 24 行、176 张 528×808 RGBA 正式帧，以及 314 组 17×25 双向动作网格。资源校验通过，全部帧透明背景、非空主体、边界留白满足检查。
- 16 向凝视的脚底统一为 y=780，人物高度为 727–728 像素；尺寸差不超过 1 像素。
- Windows CI 的源码契约测试、全部内嵌帧解码、网格插值与窗口交互测试均通过。测试 EXE 单独复制到隔离目录后启动，无外部图片依赖。
- 真实窗口测试覆盖单击、双击挥手、拖拽保持、释放恢复、捕获丢失、手机持续与退出、睡眠与唤醒、125%/225%/400% 缩放、脚底锚点与暂停恢复。
- 本机 Windows 应用控制策略阻止执行新生成的 EXE；源码编译通过不等于运行通过。
- 通过记录：[Windows CI 35840955207](https://github.com/super-idol-hub/yanan/actions/runs/35840955207)，源码提交 `64f7c34a141131072ef99965f7a45aec3896da00`。运行环境为 GitHub 托管 Windows runner，不等同于干净 Windows 10/11 的人工验收。
- 干净 Windows 10/11 人工桌面验收尚未完成。
- 没有将未执行的测试标记为通过，也未创建正式 Release。

证据保存在 `qa/evidence/`：资源清单与哈希、网格校验、24 行动作预览、隐私扫描，以及 CI 实际运行报告和渲染画面。渲染画面来自传给真实 layered window 的位图，属于自动化证据，不是人工桌面验收截图。

源码和素材包的文件名、常见凭据格式、个人本地路径及归档内容扫描通过。没有分发原始参考照片、视频、音频或歌词。

待验收项见 [人工验收表](MANUAL_ACCEPTANCE.md)：干净 Win10/Win11、深浅桌面、全部动作视觉连贯性、跨显示器与 DPI、真实重启自启行为。首发仅一套造型，同造型菜单选择为无操作；切换渲染算法经过自检，但不存在第二套官方造型可供真人切换验收。
